<!DOCTYPE html>
<html>
  <head>
    <title>LockerApplication</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <% unless current_page?(new_user_session_path) or current_page?(new_user_registration_path) or current_page?(new_user_password_path) or current_page?(edit_user_password_path)%>
      <%= render 'layouts/navigation' %>
    <% end %>
    
    <p class="notice text-1xl font-mono mx-10 my-10 text-blue-500 font-bold"><%= notice  %></p>
    <p class="alert text-1xl font-mono mx-10 my-10 text-red-500 font-bold"><%= alert %></p>
    <%= yield %>
    <%= render 'layouts/footer' %>
  </body>
</html>
